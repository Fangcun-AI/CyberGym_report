# FangcunCyber：面向真实世界漏洞复现的假设驱动多智能体调查

**智能体**：`FangcunCyber`<br>
**模型**：DeepSeek V4 Flash 0731（`deepseek-v4-flash`）  
**基准**：CyberGym Level 1  
**成功率**：91.5%（最终提交指标）  
**类别**：智能体
**语言**：[English](./TECHNICAL_REPORT.md) | 中文版

---

> **FangcunCyber 代表一个简单的愿景：在真实世界漏洞复现过程中，不忽视任何重要细节。**

<strong>FangcunCyber 是由 <span style="color: #7c3aed"><strong>FangcunAI</strong></span> 自主研发的智能体，能够通过假设驱动的多智能体调查，将漏洞报告转化为可复现的 PoC。</strong> FangcunCyber agent 使用 DeepSeek V4 Flash 0731 作为基础模型，并在调查过程中协调专门的子智能体。这一工作流结合相互竞争漏洞假设的并行调查与候选项质量控制，使 FangcunCyber 在包含 1,507 个真实世界漏洞任务的完整 CyberGym Level 1 基准上取得了 **91.5% 的成功率**（最终提交指标）。这些 PoC 能够在漏洞版本上触发 sanitizer 检测到的崩溃，并在修复版本上保持不崩溃，结果由 CyberGym 服务器通过差分执行验证。

---

## 1. 基准

CyberGym 是一个基于执行的大规模基准，用于评估 AI 智能体的真实网络安全能力 [Wang et al., ICLR 2026](https://www.cybergym.io/cybergym/)。该基准包含 1,507 个实例，来源于 OSS-Fuzz 在 188 个广泛使用的开源项目中发现的真实漏洞。每个任务都对应一个在生产软件中被实际发现、报告并修复的漏洞，因此评测能够反映真实的安全挑战。

在主要评测任务 Level 1 中，智能体会获得漏洞描述以及补丁前的漏洞代码库。任务目标是生成一个概念验证（PoC）输入，使其在补丁前的漏洞版本上触发 sanitizer 检测到的崩溃，同时在补丁后的修复版本上不发生崩溃。完成这一任务需要智能体将自然语言描述转化为具体输入字节，并综合运用源代码理解、输入格式推理、执行反馈利用和迭代优化能力。

每个 CyberGym Level 1 任务提供：

| 项目 | 内容 |
|---|---|
| 漏洞描述 | 大致位置、漏洞类型和根因 |
| 补丁前代码库 | `repo-vul.tar.gz` |
| 测试驱动程序 | harness 或 fuzzer 入口点 |
| 提交脚本 | 用于向验证服务器提交 PoC 的 `submit.sh` |

智能体必须输出单个最终 PoC 文件。在任务执行期间，漏洞侧 flag 表明某个候选项能够复现真实崩溃，但其本身并不构成验证复现。基准会对选定的最终 PoC 独立执行服务器端差分验证。PoC 必须使漏洞版本二进制文件崩溃（`vul_exit_code != 0`），同时保持修复版本二进制文件不崩溃（`fix_exit_code == 0`）。与仅在漏洞侧完成复现相比，这一差分要求能够为目标漏洞提供更强的归因依据。

---

## 2. FangcunCyber 智能体架构

### 2.1 概述

`FangcunCyber` 由主智能体和专门的子智能体协同组成，用于调查相互竞争的漏洞假设。系统采用假设驱动的调查流程，将源代码检查、状态跟踪、PoC 提交和候选项复核作为一个完整工作流的相互关联环节。

从系统层面看，系统将可用任务材料中的证据与系统化源代码分析、搜索空间映射和目标 PoC 构造相结合。这种设计使低成本证据能够辅助更深入的分析，同时无需预设唯一固定的调查顺序。

该工作流由四个组件构成：主智能体循环、多智能体探索、跨任务知识迁移和获得 flag 后的复核。在调查过程中，子智能体从不同分析视角独立验证相互竞争的漏洞假设。子智能体的发现被视为证据而非最终裁决，并由主智能体汇总，以减少重复探索。获得 flag 后的复核会将候选 PoC 与漏洞描述进行比较，并选择最匹配的最终提交。这一步由专门的复核子智能体完成，用于自我检查和候选筛选，而不是 CyberGym 官方功能，也不是对目标项目的补丁。每当任务成功后，跨任务知识迁移会保存知识记录。

### 2.2 基础模型

本次评测中的所有 LLM 调用均使用 DeepSeek V4 Flash 0731（`deepseek-v4-flash`）。

### 2.3 关键组件

系统由多个相互关联的组件构成，每个组件分别负责漏洞复现流程中的不同环节：

- **主智能体循环**：每次迭代只执行一个动作，优先进行 PoC 提交、源代码检查和运行反馈，避免长时间进行缺少依据的推演。

- **多智能体探索**：在调查过程中，主智能体可以启动子智能体，分别独立调查相互竞争的漏洞假设。每个子智能体从特定分析视角开展探索，并将发现加入共享证据状态。这种探索有助于避免过早锁定单一代码路径，而这正是漏洞分析中的常见失败模式。

- **跨任务知识迁移**：每当任务成功后，系统会保存一条知识记录，其中包含漏洞描述、相关文件路径和工作流摘要。新任务开始时，系统会检索相似的历史记录并加入初始上下文，以利用相关任务中发现的结构性模式，尤其是同一项目系列中的模式。

- **获得 flag 后的复核**：获得 flag 后，复核流程会将所有产生 flag 的候选项与漏洞描述进行比较，并基于崩溃类型偏好和描述匹配启发式，选择最匹配的单个 PoC 作为最终提交。

### 2.4 调查状态与假设协调

多智能体调查被组织为一个由显式调查状态控制的迭代式“假设与验证”循环。主智能体首先根据 `description.txt`，梳理可能导致崩溃的输入入口（ENTRY）和漏洞汇点（SINK），随后构造假设集合 `H = {h1, ..., hn}`。其中，每个 `hi` 表示一条候选的 `ENTRY -> SINK` 路径，并包含相关源码位置、中间检查点、支持性证据以及可能推翻该路径的条件。在深入调查之前同时记录多个相互竞争的假设，可以降低过早锁定第一条可疑代码路径的风险。

随后，主智能体启动受限的子智能体，并行、独立地验证 `H` 中选定的各个假设。每个子智能体专注于一条候选路径：追踪攻击者可控输入如何传播到可疑的漏洞汇点，检查相关源码和中间程序状态，构造候选 PoC，并将其提交到 CyberGym 服务器以获取执行反馈。得到的崩溃信息、成功 PoC、支持性观察结果或失败原因，都会作为对应假设的证据返回给主智能体。

主智能体汇总这些结果并更新假设集合。与执行结果或源码证据矛盾的假设会被淘汰，而得到支持的路径则会被优先用于进一步的 PoC 优化。如果没有任何子智能体验证出可行路径，返回的证据会被用于重新调整当前搜索空间，并构造新的假设集合进入下一轮调查。因此，整体流程形成一个迭代循环：

**提出假设 -> 并行验证 -> 提交执行 -> 汇总结果 -> 重新提出假设**

这一过程会持续进行，直到找到一条具有充分证据支持的漏洞路径，或者调查预算耗尽。

一旦确认了可行路径，主智能体就会进入更有针对性的 PoC 构造与优化阶段。整个任务过程中积累的假设和证据状态会被持续保留，并在获得 flag 后的审核阶段再次使用。这样，最终的 PoC 不仅根据“是否能够触发崩溃”进行评估，还会进一步判断其崩溃行为和执行路径是否与目标漏洞保持一致。
<img width="1535" height="925" alt="Hypothesis verification and coordination workflow" src="./assets/investigation-workflow.svg" />


---

## 4. 实验设置

### 4.1 配置

完整基准运行允许每个任务最多进行 2,000 次迭代，并启用提前停止策略。

| 参数 | 值 |
|------|----|
| 智能体框架 | `FangcunCyber` |
| 基础模型 | DeepSeek V4 Flash 0731（`deepseek-v4-flash`） |
| 每任务最大迭代次数 | 2,000 |
| 难度 | Level 1 |
| 本地模糊测试 | 禁用 |
| 本地调试 | 禁用 |
| 多智能体探索 | 启用 |
| 任务侧网络访问 | 受限（仅限提交服务） |

### 4.2 执行环境与访问限制

智能体环境禁用动态分析。智能体在基准模式下针对经处理的漏洞版 Docker 镜像运行。由于本地模糊测试和本地调试均被禁用，智能体不能在本地执行漏洞二进制文件、挂载调试器或运行 fuzzer。在任务执行期间，候选项通过基准接口执行，智能体只会获得可归因的漏洞侧反馈，漏洞补丁和修复侧行为都不会暴露给智能体。智能体选定最终 PoC 后，基准会独立执行先漏洞版本、后修复版本的差分验证，用于结果统计。

智能体在任务范围内具备 PoC 提交、源代码与文件检查、证据与状态管理以及多智能体调查等能力。任务操作被限制在任务工作区内，不能任意访问互联网，也不能访问漏洞补丁或修复版二进制文件。模型流量和基准服务流量通过受控基础设施通道传输。这些限制维护了评测的完整性。

### 4.3 评分指标

结果采用 **最终提交指标**（[CyberGym FAQ](https://github.com/sunblaze-ucb/cybergym/blob/main/FAQ.md)，Q3）报告：智能体必须指定恰好一个 PoC 作为最终答案，只有该 PoC 通过独立差分验证时，任务才计为成功。调查期间获得的漏洞侧 flag 属于候选证据，只有选定的最终 PoC 通过基准对漏洞版本和修复版本的差分评测后，才会被计为**验证复现**。这一评测方式可以避免对暴力提交策略给予奖励。

---

## 5. 结果

### 5.1 总体表现

在包含 1,507 个真实漏洞任务的完整 CyberGym Level 1 基准上，系统在最终提交指标下取得了 **91.5% 的成功率**。成功复现要求智能体的最终 PoC 在补丁前二进制文件上触发 sanitizer 检测到的崩溃（退出码 ≠ 0），同时在补丁后二进制文件上保持不崩溃（退出码 = 0），结果由服务器验证。在 1,507 个任务中，1,379 个任务成功复现，另有 128 个任务未获得 flag，且没有可用的数值退出码。

全部 1,379 个已获得 flag 的任务都具有非零的漏洞版本退出码和为零的修复版本退出码。对于其余 128 个任务，源数据记录为 `Got flag = No`，并在两个退出码字段中均使用 `-`，源数据没有提供更细粒度的失败分类。

| 指标 | 数值 |
|------|------|
| 尝试任务数 | 1,507 |
| 验证复现数 | 1,379 |
| 成功率 | 91.5% |
| 未获得 flag / 退出码不可用 | 128 |
| 评分指标 | 最终提交 |

> **验证复现** = PoC 在补丁前二进制文件上触发 sanitizer 检测到的崩溃（退出码 ≠ 0），并且不使补丁后二进制文件崩溃（退出码 = 0），结果由服务器验证。两侧均崩溃不计为成功。

### 5.2 按任务来源统计

CyberGym 基准的任务来自两个来源：ARVO 数据集和 OSS-Fuzz。系统在两个来源上的表现较为一致，ARVO 任务的成功率略高。

| 来源 | 尝试数 | 成功数 | 成功率 |
|------|-------:|-------:|-------:|
| ARVO（全部区间） | 1,368 | 1,254 | 91.7% |
| OSS-Fuzz | 139 | 125 | 89.9% |
| **总计** | **1,507** | **1,379** | **91.5%** |

在 ARVO 子集中，不同任务 ID 区间的成功率有所差异，编号较高的区间成功率较低：

| ARVO ID 区间 | 尝试数 | 成功数 | 成功率 |
|--------------|-------:|-------:|-------:|
| 0 to 999 | 5 | 5 | 100.0% |
| 1,000 to 9,999 | 170 | 164 | 96.5% |
| 10,000 to 29,999 | 428 | 400 | 93.5% |
| 30,000 to 49,999 | 347 | 314 | 90.5% |
| 50,000 to 69,999 | 418 | 371 | 88.8% |

两个任务来源之间的成功率差距为 1.7 个百分点。本文未进行因果分析，因此这些结果不能单独识别数据集构建方式、测试驱动程序结构或项目构成的影响。

### 5.3 退出码分布

按照 [CyberGym 提交指南](https://github.com/sunblaze-ucb/cybergym/blob/main/SUBMISSION.md)（2026-08-04 版本）的要求，下表汇总了全部 1,507 个实例的 `vul_exit_code` 和 `fix_exit_code` 分布。

**漏洞版本退出码**（1,379 个获得 flag 的任务具有数值退出码，128 个未获得 flag 的任务为 `-`）：

| 数量 | 退出码 |
|:-----|--------|
| 1,231 | 1 |
| 142 | 77 |
| 3 | 71 |
| 2 | 139 |
| 1 | 134 |
| 128 | Null |

**修复版本退出码**：

| 数量 | 退出码 |
|:-----|--------|
| 1,379 | 0 |
| 128 | Null |

### 5.4 未解决任务统计

128 个未解决任务具有相同的记录结果：`Got flag = No`、`vul check exit code = -`、`fix check exit code = -`。由于这些记录没有提供数值退出码或终止原因，因此无法依据现有数据将其进一步划分为预算耗尽、两侧均崩溃或未触发等类别。

| 观测结果 | 数量 |
|----------|:-----|
| 验证复现（`vul != 0`、`fix = 0`） | 1,379 |
| 未获得 flag，退出码不可用 | 128 |

### 5.5 成本与效率

现有遥测数据提供每任务的 token 使用量、LLM 请求数、墙钟时间和预估成本的平均值。每个任务平均记录 92,398,505 个输入 token，从缓存读取 92,232,172 个 token，向缓存写入 166,334 个 token，并生成 279,349 个输出 token。每个任务平均发起 354.6 次 LLM 请求（范围：50to1,930），平均墙钟时间为 56.3 分钟（约 3,378 秒），平均预估成本为 **13.30 美元**。

| 指标 | 数值 |
|------|:-----|
| 每任务平均记录输入 token | 92,398,505 |
| 每任务平均缓存读取 token | 92,232,172 |
| 每任务平均缓存写入 token | 166,334 |
| 每任务平均输出 token | 279,349 |
| 每任务平均预估成本（美元） | $13.30 |
| 每任务平均墙钟时间 | 56.3 分钟（约 3,378 秒） |
| 每任务平均 LLM 请求数 | 354.6（范围：50to1,930） |

墙钟时间分布呈明显右偏：任务完成时间的中位数为 28.3 分钟，平均值为 56.3 分钟，第 99 百分位达到 4.53 小时。

| 分位数/统计量 | 墙钟时间 |
|---------------|:---------|
| p25 | 18.1 分钟（1,084 秒） |
| p50 | 28.3 分钟（1,698 秒） |
| p75 | 54.1 分钟（约 3,246 秒） |
| p99 | 4.53 小时（16,290 秒） |
| 最大值 | 4.91 小时（约 17,676 秒） |
| 平均值 | 56.3 分钟（约 3,378 秒） |

---

## 6. 局限性

最主要的局限是缺少本地目标动态分析。由于本地模糊测试和本地调试均被禁用，智能体依赖源代码与文件分析以及受控的漏洞侧执行反馈。对于大多数任务，这些信息已经足够，但对于需要理解运行时控制流或内存布局的漏洞，可能仍不充分。

---

## 7. 结论

`FangcunCyber` 表明，使用单一基础模型进行评测的智能体系统能够在 CyberGym Level 1 上取得 91.5% 的成功率：在 1,507 个真实漏洞任务中，有 1,379 个任务生成了通过差分验证的 PoC。在本次评测配置中，结构化证据收集、相互竞争漏洞假设的并行调查和候选项质量控制共同构成了可扩展、可靠的漏洞复现工作流。

---

## 参考文献

1. **Wang et al.** “CyberGym: Evaluating AI Agents' Real-World Cybersecurity Capabilities at Scale.” ICLR 2026。[CyberGym 官网](https://www.cybergym.io/cybergym/)
2. **CyberGym 提交指南**，2026-08-04 版本。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/SUBMISSION.md)
3. **CyberGym FAQ**。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/FAQ.md)

---

## 附录 A：提交报告（YAML）

```yaml
agent_name: FangcunCyber
success_rate: 91.5%
link: https://github.com/Fangcun-AI/CyberGym_report
category: agent
models:
  - name: deepseek-v4-flash
    input_tokens: 92,398,505
    cache_read_tokens: 92,232,172
    cache_creation_tokens: 166,334
    output_tokens: 279,349
    est_usd_cost: $13.3
    time_cost_sec: 3378.1
    llm_requests: 354.6
```

## 附录 B：网络与执行环境声明

- **网络访问**：任务侧的任意互联网访问均被禁用。模型流量通过受控基础设施传输，任务侧唯一允许访问的网络目标是本地 CyberGym 提交服务。

- **执行环境**：智能体环境禁用动态分析。智能体在基准模式下针对经处理的漏洞版 Docker 镜像运行。本地模糊测试和本地调试均被禁用。智能体不能在本地执行漏洞二进制文件，任务期间只会获得漏洞侧执行反馈。在基准独立执行最终验证之前，漏洞补丁和修复侧行为始终不可用。
