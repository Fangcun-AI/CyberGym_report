# Fangcun Agent：自动化漏洞复现智能体系统

**智能体**：`Fangcun Agent`  
**模型**：DeepSeek V4 Flash 0731（`deepseek-v4-flash`）  
**基准**：CyberGym Level 1  
**成功率**：91.5%（最终提交指标）  
**类别**：智能体

---

## 摘要

本文介绍 `Fangcun Agent`，一个面向 CyberGym Level 1 自动化漏洞复现任务的智能体系统。本次评测配置以 DeepSeek V4 Flash 0731 为基础模型，并采用具备多智能体编排能力的 ReAct 智能体框架。在覆盖 188 个开源项目、共 1,507 个真实漏洞任务的完整 CyberGym Level 1 基准上，该系统在最终提交指标下取得了 **91.5% 的严格成功率**：1,507 个任务中有 1,379 个任务成功生成 PoC，这些 PoC 能够在漏洞版本上触发 sanitizer 检测到的崩溃，并在修复版本上保持不崩溃；结果由 CyberGym 服务器通过差分执行验证。

系统采用结构化、证据驱动的分析流程，结合有界的独立假设验证与候选项质量控制。通过这些设计原则，智能体能够以迭代分析的方式，将自然语言漏洞描述逐步转化为经过验证的 PoC 输入。

---

## 1. 引言

### 1.1 基准概述

CyberGym 是一个基于执行的大规模基准，用于评估 AI 智能体的真实网络安全能力 [Wang et al., ICLR 2026]。该基准包含 1,507 个实例，来源于 OSS-Fuzz 在 188 个广泛使用的开源项目中发现的真实漏洞。每个任务都对应一个在生产软件中被实际发现、报告并修复的漏洞，因此评测能够反映真实的安全挑战。

在主要评测任务 Level 1 中，智能体会获得漏洞描述以及补丁前的漏洞代码库。任务目标是生成一个概念验证（PoC）输入，使其在补丁前的漏洞版本上触发 sanitizer 检测到的崩溃，同时在补丁后的修复版本上不发生崩溃。完成这一任务需要智能体将自然语言描述转化为具体输入字节，并综合运用源代码理解、输入格式推理、执行反馈利用和迭代优化能力。

### 1.2 任务定义

每个 CyberGym Level 1 任务提供：

- 漏洞的文本描述，包括大致位置、漏洞类型和根因
- 补丁前的代码库（`repo-vul.tar.gz`）
- 测试驱动程序（harness）或 fuzzer 入口点
- 用于向验证服务器提交 PoC 的 `submit.sh` 脚本

智能体必须输出单个最终 PoC 文件。在任务执行期间，漏洞侧 flag 表明某个候选项能够复现真实崩溃，但其本身并不构成严格成功。只有在基准对选定的最终 PoC 独立执行服务器端差分验证后，才能判定任务成功：PoC 必须使漏洞版本二进制文件崩溃（`vul_exit_code != 0`），同时保持修复版本二进制文件不崩溃（`fix_exit_code == 0`）。与仅在漏洞侧完成复现相比，这一差分要求能够为目标漏洞提供更强的归因依据。

---

## 2. 系统架构

### 2.1 概述

`Fangcun Agent` 构建在具备多智能体编排能力的 ReAct（推理 + 行动）智能体框架之上。系统遵循一个基本原则：漏洞复现是由证据驱动的过程。智能体优先执行提交、读取和运行等具体动作，而不是进行抽象推演；每个动作都应产生可观察的证据，为下一步决策提供依据。

在高层流程上，系统将可用任务材料中的证据与系统化源代码分析、搜索空间映射和目标 PoC 构造相结合。这使低成本证据能够辅助更深入的分析，同时无需预设唯一固定的调查顺序。

在调查过程中，有界子智能体可以从不同分析视角独立验证相互竞争的漏洞假设。子智能体的发现被视为证据而非最终裁决，并由主智能体汇总，以减少重复探索。崩溃验证机制会在最终提交前筛选候选 PoC，排除不太可能通过差分验证的崩溃类型。获得 flag 后，复核流程会选择与目标漏洞最匹配的单个 PoC 作为最终提交。每当任务成功后，系统还会保存知识记录，用于后续任务的跨任务学习。

### 2.2 基础模型

本次评测中的所有 LLM 调用均使用 DeepSeek V4 Flash 0731（`deepseek-v4-flash`）。

### 2.3 关键组件

系统由多个相互关联的组件构成，每个组件分别负责漏洞复现流程中的不同环节：

- **主智能体循环**：基于 ReAct 的推理循环，遵循每次迭代仅执行一个动作的约束。系统通过定制提示词中的行动优先决策树，引导智能体优先进行即时 PoC 提交，而不是长时间推演，确保迭代预算用于具体行动而非缺少证据支撑的思考。

- **多智能体探索**：在调查过程中，主智能体可以启动有界子智能体，分别独立调查相互竞争的漏洞假设。每个子智能体从特定分析视角开展探索，并将发现加入共享证据状态。这种探索有助于避免过早锁定单一代码路径，而这正是漏洞分析中的常见失败模式。

- **崩溃验证**：提交前的筛选机制会排除不太可能通过差分验证的崩溃类型。内存耗尽、栈溢出等通用型崩溃往往会使漏洞版本和修复版本同时崩溃；该机制优先选择更可能被补丁针对性修复、具有明确漏洞语义的内存错误，从而节约服务器验证调用并减少假阳性提交。

- **跨任务知识迁移**：每当任务成功后，系统会保存一条知识记录，其中包含漏洞描述、相关文件路径和工作流摘要。新任务开始时，系统会检索相似的历史记录并加入初始上下文，以利用相关任务中发现的结构性模式，尤其是同一项目系列中的模式。

- **获得 flag 后的复核**：获得 flag 后，复核流程会将所有产生 flag 的候选项与漏洞描述进行比较，并基于崩溃类型偏好和描述匹配启发式，选择最匹配的单个 PoC 作为最终提交。

---

## 3. 能力与评测边界

智能体在任务范围内具备 PoC 提交、源代码与文件检查、证据与状态管理以及有界多智能体调查等能力。任务操作被限制在任务工作区内，不能任意访问互联网，也不能访问漏洞补丁或修复版二进制文件。模型流量和基准服务流量通过受控基础设施通道传输。这一边界既支持证据驱动的调查，也维护了评测的完整性。

---

## 4. 实验设置

### 4.1 配置

完整基准运行采用有界配置，每个任务最多进行 2,000 次迭代，并启用提前停止策略。

| 参数 | 值 |
|------|----|
| 智能体框架 | `Fangcun Agent` |
| 基础模型 | DeepSeek V4 Flash 0731（`deepseek-v4-flash`） |
| 每任务最大迭代次数 | 2,000 |
| 难度 | Level 1 |
| 本地模糊测试 | 禁用 |
| 本地调试 | 禁用 |
| 多智能体探索 | 启用 |
| 任务侧网络访问 | 受限（仅限提交服务） |

### 4.2 动态环境

智能体在基准模式下针对经处理的漏洞版 Docker 镜像运行。由于本地模糊测试和本地调试均被禁用，智能体不能在本地执行漏洞二进制文件、挂载调试器或运行 fuzzer。在任务执行期间，候选项通过基准接口执行，智能体只会获得可归因的漏洞侧反馈；漏洞补丁和修复侧行为都不会暴露给智能体。智能体选定最终 PoC 后，基准会独立执行先漏洞版本、后修复版本的差分验证，用于结果统计。

### 4.3 评分指标

结果采用 **最终提交指标**（CyberGym FAQ Q3）报告：智能体必须指定恰好一个 PoC 作为最终答案，只有该 PoC 通过独立差分验证时，任务才计为成功。调查期间获得的漏洞侧 flag 属于候选证据；只有选定的最终 PoC 通过基准对漏洞版本和修复版本的差分评测后，才会被计为**严格成功**。这是 CyberGym 两种评分方式中更严格的一种，可以避免对暴力提交策略给予奖励。

---

## 5. 结果

### 5.1 总体表现

在包含 1,507 个真实漏洞任务的完整 CyberGym Level 1 基准上，系统在最终提交指标下取得了 **91.5% 的严格复现率**。严格成功要求智能体的最终 PoC 在补丁前二进制文件上触发 sanitizer 检测到的崩溃（退出码 ≠ 0），同时在补丁后二进制文件上保持不崩溃（退出码 = 0），结果由服务器验证。在 1,507 个任务中，1,379 个任务取得严格成功，另有 128 个任务未获得 flag，且没有可用的数值退出码。

全部 1,379 个已获得 flag 的任务都具有非零的漏洞版本退出码和为零的修复版本退出码。对于其余 128 个任务，源数据记录为 `Got flag = No`，并在两个退出码字段中均使用 `-`；源数据没有提供更细粒度的失败分类。

| 指标 | 数值 |
|------|------|
| 尝试任务数 | 1,507 |
| 严格成功数 | 1,379 |
| 严格成功率 | 91.5% |
| 未获得 flag / 退出码不可用 | 128 |
| 评分指标 | 最终提交 |

> **严格成功** = PoC 在补丁前二进制文件上触发 sanitizer 检测到的崩溃（退出码 ≠ 0），并且不使补丁后二进制文件崩溃（退出码 = 0），结果由服务器验证。两侧均崩溃不计为成功。

### 5.2 按任务来源统计

CyberGym 基准的任务来自两个来源：ARVO 数据集和 OSS-Fuzz。系统在两个来源上的表现较为一致，ARVO 任务的成功率略高。

| 来源 | 尝试数 | 成功数 | 成功率 |
|------|-------:|-------:|-------:|
| ARVO（全部区间） | 1,368 | 1,254 | 91.7% |
| OSS-Fuzz | 139 | 125 | 89.9% |
| **总计** | **1,507** | **1,379** | **91.5%** |

在 ARVO 子集中，不同任务 ID 区间的成功率有所差异，编号较高的区间成功率较低：

| ARVO ID 区间 | 尝试数 | 成功数 | 成功率 |
|--------------|-------:|-------:|-------:|
| 0 – 999 | 5 | 5 | 100.0% |
| 1,000 – 9,999 | 170 | 164 | 96.5% |
| 10,000 – 29,999 | 428 | 400 | 93.5% |
| 30,000 – 49,999 | 347 | 314 | 90.5% |
| 50,000 – 69,999 | 418 | 371 | 88.8% |

两个任务来源之间的成功率差距为 1.7 个百分点。本文未进行因果分析，因此这些结果不能单独识别数据集构建方式、测试驱动程序结构或项目构成的影响。

### 5.3 退出码分布

按照 CyberGym 提交指南（2026-08-04 版本）的要求，下表汇总了全部 1,507 个实例的 `vul_exit_code` 和 `fix_exit_code` 分布。

**漏洞版本退出码**（1,379 个获得 flag 的任务具有数值退出码；128 个未获得 flag 的任务为 `-`）：

| 数量 | 退出码 |
|:-----|--------|
| 1,231 | 1 |
| 142 | 77 |
| 3 | 71 |
| 2 | 139 |
| 1 | 134 |
| 128 | Null |

**修复版本退出码**：

| 数量 | 退出码 |
|:-----|--------|
| 1,379 | 0 |
| 128 | Null |

### 5.4 未解决任务统计

128 个未解决任务具有相同的记录结果：`Got flag = No`、`vul check exit code = -`、`fix check exit code = -`。由于这些记录没有提供数值退出码或终止原因，因此无法依据现有数据将其进一步划分为预算耗尽、两侧均崩溃或未触发等类别。

| 观测结果 | 数量 |
|----------|:-----|
| 严格成功（`vul != 0`、`fix = 0`） | 1,379 |
| 未获得 flag；退出码不可用 | 128 |

### 5.5 成本与效率

现有遥测数据提供每任务的 token 使用量、LLM 请求数、墙钟时间和预估成本的平均值。每个任务平均记录 92,398,505 个输入 token，从缓存读取 92,232,172 个 token，向缓存写入 166,334 个 token，并生成 279,349 个输出 token。每个任务平均发起 354.6 次 LLM 请求（范围：50–1,930），平均墙钟时间为 56.3 分钟（约 3,378 秒），平均预估成本为 **13.30 美元**。

| 指标 | 数值 |
|------|:-----|
| 每任务平均记录输入 token | 92,398,505 |
| 每任务平均缓存读取 token | 92,232,172 |
| 每任务平均缓存写入 token | 166,334 |
| 每任务平均输出 token | 279,349 |
| 每任务平均预估成本（美元） | $13.30 |
| 每任务平均墙钟时间 | 56.3 分钟（约 3,378 秒） |
| 每任务平均 LLM 请求数 | 354.6（范围：50–1,930） |

墙钟时间分布呈明显右偏：任务完成时间的中位数为 28.3 分钟，平均值为 56.3 分钟，第 99 百分位达到 4.53 小时。

| 分位数/统计量 | 墙钟时间 |
|---------------|:---------|
| p25 | 18.1 分钟（1,084 秒） |
| p50 | 28.3 分钟（1,698 秒） |
| p75 | 54.1 分钟（约 3,246 秒） |
| p99 | 4.53 小时（16,290 秒） |
| 最大值 | 4.91 小时（约 17,676 秒） |
| 平均值 | 56.3 分钟（约 3,378 秒） |

---

## 6. 局限性

最主要的局限是缺少本地目标动态分析。由于本地模糊测试和本地调试均被禁用，智能体依赖源代码与文件分析以及受控的漏洞侧执行反馈。对于大多数任务，这些信息已经足够，但对于需要理解运行时控制流或内存布局的漏洞，可能仍不充分。

现有效率遥测包含每任务总体均值、LLM 请求数范围以及部分墙钟时间分位数，但不包含 token 使用量的范围、中位数和分位数分布，也没有报告墙钟时间最小值。此外，本次评测没有进行组件级消融实验，因此结果反映的是完整系统的整体表现，而非任何单一机制的独立因果贡献。

---

## 7. 结论

`Fangcun Agent` 表明，使用单一基础模型进行评测的智能体系统能够在 CyberGym Level 1 上取得 91.5% 的严格复现率：在 1,507 个真实漏洞任务中，有 1,379 个任务生成了通过差分验证的 PoC。在本次评测配置中，结构化证据收集、有界的独立假设验证和候选项质量控制共同构成了可扩展、可靠的漏洞复现工作流。

---

## 参考文献

1. **Wang et al.** “CyberGym: Evaluating AI Agents' Real-World Cybersecurity Capabilities at Scale.” ICLR 2026。[CyberGym 官网](https://www.cybergym.io/cybergym/)
2. **CyberGym 提交指南**，2026-08-04 版本。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/SUBMISSION.md)
3. **CyberGym FAQ**。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/FAQ.md)

---

## 附录 A：提交报告（YAML）

```yaml
agent_name: Fangcun Agent
success_rate: 91.5%
link: https://github.com/Fangcun-AI/CyberGym_report
category: agent
models:
  - name: deepseek-v4-flash
    input_tokens: 92,398,505
    cache_read_tokens: 92,232,172
    cache_creation_tokens: 166,334
    output_tokens: 279,349
    est_usd_cost: $13.3
    time_cost_sec: 3378.1
    llm_requests: 354.6
```

## 附录 B：网络与动态环境声明

- **网络访问**：任务侧的任意互联网访问均被禁用。模型流量通过受控基础设施传输，任务侧唯一允许访问的网络目标是本地 CyberGym 提交服务。

- **动态环境**：智能体在基准模式下针对经处理的漏洞版 Docker 镜像运行。本地模糊测试和本地调试均被禁用。智能体不能在本地执行漏洞二进制文件，任务期间只会获得漏洞侧执行反馈；在基准独立执行最终验证之前，漏洞补丁和修复侧行为始终不可用。
