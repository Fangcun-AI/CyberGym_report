# Fangcun Agent：CyberGym 上的多智能体漏洞复现

**智能体**：`Fangcun Agent`  
**模型**：DeepSeek V4 Flash 0731（`deepseek-v4-flash`）  
**基准**：CyberGym Level 1  
**成功率**：91.5%（最终提交指标）  
**类别**：智能体

**处理流程**：定位 → 多样化 → 证伪 → 裁决 → 重新对齐/恢复 → 复核 → 留存

---

## 摘要

`Fangcun Agent` 是一个基于 ReAct 的 CyberGym Level 1 自动化漏洞复现系统。在本次评测配置中，每个由模型驱动的角色均使用 DeepSeek V4 Flash 0731。每次调查首先由定位智能体识别相关源代码区域和候选输入面。主智能体基于这一聚焦后的上下文形成多个漏洞假设，将选定假设交给限定范围的验证智能体，并在比较所得证据后决定下一项检查。对于长时间运行的调查，上下文恢复、针对产生 flag 的候选项设置的独立复核阶段，以及保留任务内证据和选定跨任务经验的共享记录，为调查提供支持。

在来自 188 个开源项目的 1,507 个真实漏洞任务中，系统解决了 1,379 个，在最终提交指标下取得 **91.5% 的严格成功率**。最终 PoC 只有在漏洞版本上触发 sanitizer 检测到的崩溃、同时在修复版本上正常执行，才计为成功。任务期间的反馈仅限于漏洞侧执行结果；CyberGym 通过在两个版本上运行选定的 PoC 来确定最终结果。

---

## 1. 引言

### 1.1 基准概述

CyberGym 是一个基于执行的基准，用于评估 AI 智能体处理现实世界网络安全任务的能力 [Wang et al., ICLR 2026]。其 1,507 个基准实例来源于 OSS-Fuzz 在 188 个开源项目中发现的漏洞。每个实例都代表一个在生产软件中被发现、报告并随后修复的漏洞。

Level 1 是该基准的主要任务。给定漏洞描述和补丁前的代码库，智能体必须构造一个概念验证（PoC）输入，使其在补丁前版本上触发 sanitizer 检测到的崩溃，同时在补丁后版本上正常执行。完成这一任务需要定位相关实现，推理可接受的输入结构和失败路径，构造具体输入字节，并依据执行反馈修订候选项。

### 1.2 任务定义

每个 CyberGym Level 1 任务提供：

- 漏洞的文本描述，包括大致位置、漏洞类型和根因
- 补丁前的代码库（`repo-vul.tar.gz`）
- 测试驱动程序（harness）或 fuzzer 入口点
- 用于向验证服务器提交 PoC 的 `submit.sh` 脚本

任务要求交付单个最终 PoC 文件。漏洞侧 flag 能够确认已提交的候选项在调查期间产生了真实崩溃，但不能据此判定基准任务成功。CyberGym 会在两个版本上独立评测选定的 PoC。严格成功要求 `vul_exit_code != 0` 且 `fix_exit_code == 0`，从而排除在修复版本上同样失败的候选项。

### 1.3 设计动机

长时间运行的任务对上下文管理提出了相互竞争的要求。将完整源代码树与不断增长的执行历史一并保留，可能稀释与所报告缺陷相关的证据；过度精简又可能在检查之前丢弃仍然合理的路径。过早认定单一因果解释，同样可能排除可行的输入构造或失效机制。

Fangcun Agent 首先将当前工作集限定为与任务有关的源代码区域、输入面和未解决问题。基于这份聚焦后的记录，系统维护多个可检验的解释。限定范围的智能体调查选定解释，并返回由观测结果支持的候选项，或具体说明哪些内容无法得到证实。协调器比较这些报告，并选择下一项最能区分不同假设的行动。

长期调查也可能偏离所报告的漏洞，或积累内部不一致的状态。系统会将当前路径与任务描述进行比较，并在必要时根据已记录的证据重建精简的调查状态。

---

## 2. 系统架构

### 2.1 概述

`Fangcun Agent` 将一个主 ReAct（推理 + 行动）循环与读取并写入共享任务状态的专用智能体相结合。定位结果界定后续推理可使用的相关文件、函数、输入面和未解决问题。主智能体根据这些材料形成候选解释，将选定解释交给有界验证智能体，并把验证报告提交给协调器比较。

对于长时间运行的任务，可以将当前调查重新对齐到原始描述，或根据已记录的材料重建调查状态。候选项产生漏洞侧 flag 后，独立复核智能体会在最终选择之前检查其可复现性以及与所报告漏洞的一致性。共享状态保留当前任务的证据，并可在遇到相关任务时提供以往成功调查的记录。

专用智能体的输出用于支持正在进行的调查。定位结果、验证报告、协调器决策和复核建议均属于中间证据，并不构成基准结果。

### 2.2 基础模型

本次评测中的所有 LLM 调用均使用 DeepSeek V4 Flash 0731（`deepseek-v4-flash`）。

| 值 | 参数 |
|----|------|
| `deepseek-v4-flash` | 模型 |
| LangChain + LangGraph | 框架 |

本次评测系统中每个由模型驱动的角色都使用同一基础模型。角色行为受分配的目标、职责和必需输出约束。因此，由各自具有明确职责范围的智能体生成的报告，不被视为统计独立的模型样本。

### 2.3 组件与输出

下表汇总了各阶段的预期输出，以及各阶段旨在应对的工程风险。

| 阶段 | 与决策相关的输出 | 主要应对风险 |
|------|------------------|--------------|
| 上下文定位 | 紧凑且与源代码关联的证据图谱 | 长上下文中的信息稀释 |
| 假设形成 | 多个可证伪的因果解释 | 过早收敛 |
| 有界验证通道 | 可比较的支持、反证、候选项或阻塞证据 | 分支失控与分析模糊 |
| 证据裁决 | 经优先排序和剪枝的方向，以及尚未解决的方向 | 叙事驱动的选择 |
| 重新对齐与恢复 | 重新锚定或重建的调查状态 | 调查轨迹漂移与上下文失效 |
| 获得 flag 后的复核 | 归因最充分的可复现候选项 | 偶发的漏洞侧崩溃 |
| 双层记忆 | 共享任务证据与可复用经验 | 重复工作与重复发现 |

#### 2.3.1 智能体辅助的上下文定位

专用定位智能体在严格限定的目标下检查任务材料，并生成与源代码关联的记录。这些记录标识与任务相关的源代码区域、候选输入面、可能的失效区域、行为线索和尚未解决的证据缺口。

由此得到的图谱能够减少后续智能体必须保持在活跃上下文中的材料，而无须改变基础模型的上下文窗口。由于定位结果会写入共享任务状态，因此即使超出任何单个智能体轨迹的上下文范围，这些结果仍然可用。

#### 2.3.2 可证伪假设与有界验证通道

主调查流程根据定位后的证据，记录若干合理的从输入入口到漏洞汇点的解释。每个假设都会明确哪些观测能够支持它，以及哪些证据能够反驳它。

选定假设由各自具有明确职责范围的有界验证智能体进行评估。每个智能体必须返回由具体观测支持的可构造候选项，或者明确指出哪些内容无法得到证实的阻塞说明。未包含上述任一项的报告，对决策的价值有限。可构造的候选项能够支持所提路径的可达性，但不能证明该候选项满足任务要求。

#### 2.3.3 证据裁决

协调器根据源代码、观测到的漏洞侧行为、支持证据与反证、缺失的检查点以及尚未解决的前提，对验证报告进行评估。协调器依据这份记录确定假设优先级、拒绝已被证据否定的路径、保留尚未解决的替代方向，或选择另一项能够区分不同假设的检查。观测到崩溃只能证明某条执行路径已被触达；要将该崩溃归因于任务描述中的漏洞，还需要额外证据。

#### 2.3.4 描述重新对齐与上下文恢复

在长期调查期间，系统会将当前路径、观测到的崩溃行为和现有解释与原始漏洞描述进行比较。实质性不匹配可能触发重新定位源代码或修订当前假设。当积累的历史过多或内部不一致时，恢复组件会根据任务材料、共享证据、未解决假设和近期记录的动作，重建精简的调查状态。重建后的状态为后续分析提供紧凑的工作记录，同时不改变模型原生的上下文限制。

#### 2.3.5 获得 flag 后的独立复核

候选项获得漏洞侧 flag 后，独立复核智能体会检查符合条件的候选项在漏洞侧的可复现性，并将其崩溃证据与漏洞描述进行比较，然后推荐一个候选项作为最终提交。复核智能体无法检查补丁或修复侧结果。其评估用于排除漏洞侧行为看似偶发或与所报告缺陷不够一致的候选项。最终差分验证仍由 CyberGym 负责。

#### 2.3.6 双层记忆与跨任务经验

`Fangcun Agent` 在两个时间尺度上维护记录。在任务内，共享状态保存定位结果、矛盾证据、假设结果和候选项观测记录，供其他角色使用。成功的调查也可能生成精简的经验记录，以便在相关任务中检索。

检索到的经验提供的是以往调查指导，而非当前任务中已经成立的事实。其适用性必须根据当前任务材料重新验证；经验检索不能替代假设检验或基准验证。该过程遵循 **调查 → 提炼 → 复用 → 重新验证**，不会训练或修改基础模型的参数。

### 2.4 调查状态与假设协调

多智能体调查被组织为一个由显式调查状态控制的迭代式“假设—验证”循环。主智能体首先根据 `description.txt`，梳理可能导致崩溃的输入入口（ENTRY）和漏洞汇点（SINK），随后构造假设集合 `H = {h1, ..., hn}`。其中，每个 `hi` 表示一条候选的 `ENTRY -> SINK` 路径，并包含相关源代码位置、中间检查点、支持性证据以及可能证伪该路径的条件。在深入调查之前记录多个相互竞争的假设，可以降低过早认定第一条可疑代码路径的风险。

随后，主智能体启动有界子智能体，分别独立验证 `H` 中选定的每个假设。每个子智能体专注于一条候选路径：追踪攻击者可控输入如何传播到可疑的漏洞汇点，检查相关源代码和中间状态，构造候选 PoC，并将其提交到 CyberGym 服务器以获取执行反馈。得到的崩溃信息、成功 PoC、支持性观测或失败原因，都会作为对应假设的证据返回给主智能体。

主智能体汇总这些结果并更新假设集合。与执行结果或源代码证据矛盾的假设会被淘汰，而得到支持的路径则会被优先用于进一步优化 PoC。如果没有任何子智能体验证出可行路径，返回的证据会被用于修订当前搜索空间，并构造新的假设集合进入下一轮调查。因此，整体过程遵循迭代式 **提出假设 → 并行验证 → 提交执行 → 汇总结果 → 重新提出假设** 循环，直到识别出证据足够充分的漏洞路径，或调查预算耗尽。

一旦确认可行路径，主智能体便会转入有针对性的 PoC 构造与优化。累积的假设和证据状态会在整个任务期间保留，并在获得 flag 后的复核中再次使用。这样，对产生 flag 的 PoC 进行评估时，不仅会考虑它是否触发崩溃，还会考虑其崩溃行为和执行路径是否与正在调查的漏洞一致。

<img width="1535" height="1024" alt="image" src="https://github.com/user-attachments/assets/8f5d98ac-36cd-407b-91dd-c06078ea3d0c" />


---

## 3. 能力与评测边界

智能体的操作范围包括源代码与任务文件检查、调查状态管理、限定范围的模型驱动分析以及 PoC 提交。所有面向任务的操作均限制在任务工作区内。智能体无法任意访问互联网，也无法获取漏洞补丁和修复版二进制文件；模型流量和基准服务通信均通过受控基础设施通道传输。

这些限制统一适用于定位智能体、验证智能体、协调器和复核智能体。每个角色均以任务提供的材料和漏洞侧证据为依据。跨任务记录仅包含以往的调查模式，不包含修复侧事实或任务答案；任何检索到的信息都必须结合当前任务重新评估。

---

## 4. 实验设置

### 4.1 配置

每个基准任务最多允许 2,000 次迭代，并采用提前停止策略。

| 参数 | 值 |
|------|----|
| 智能体框架 | `Fangcun Agent` |
| 基础模型 | DeepSeek V4 Flash 0731（`deepseek-v4-flash`） |
| 每任务最大迭代次数 | 2,000 |
| 难度 | Level 1 |
| 本地模糊测试 | 禁用 |
| 本地调试 | 禁用 |
| 多智能体探索 | 启用 |
| 任务侧网络访问 | 受限（仅限提交服务） |

### 4.2 动态环境

评测在基准模式下使用经过净化处理的漏洞版 Docker 镜像。由于本地模糊测试和调试均被禁用，智能体不能在本地执行漏洞版二进制文件、挂载调试器或运行 fuzzer。所有候选项的执行都通过基准接口进行，该接口在任务期间提供漏洞侧执行反馈。智能体无法获取补丁和修复侧行为。

选定最终 PoC 后，CyberGym 会在漏洞版本和修复版本上分别执行该 PoC，以进行结果统计。

### 4.3 评分指标

评测采用 CyberGym FAQ Q3 中描述的**最终提交**指标。对于每个任务，智能体指定且仅指定一个 PoC 作为最终答案。调查期间获得的漏洞侧 flag 是中间信号，其本身不计入得分。CyberGym 按照第 1.2 节的严格成功判据确定结果。在 CyberGym 记录的两种评分方法中，最终提交是更严格的一种，不会因为提交多个候选项直至其中一个成功而给予分数。

---

## 5. 结果

### 5.1 总体表现

系统在 1,507 个 CyberGym Level 1 任务中解决了 1,379 个，按最终提交指标计算，对应 **91.5% 的严格成功率**。其余 128 个任务没有产生 flag，也没有报告数值退出码。

每个已解决任务的结果记录都包含非零的漏洞版本退出码和为零的修复版本退出码。在其他 128 条记录中，`Got flag = No`，且两个退出码字段均为 `-`。现有数据未对这些未解决案例作进一步分类。

| 指标 | 数值 |
|------|------|
| 尝试任务数 | 1,507 |
| 严格成功数 | 1,379 |
| 严格成功率 | 91.5% |
| 未获得 flag / 退出码不可用 | 128 |
| 评分指标 | 最终提交 |

> **严格成功**：选定的 PoC 满足第 1.2 节的差分判据。漏洞版本和修复版本均崩溃的结果不计为成功。

### 5.2 按任务来源统计

基准语料包含来自 ARVO 数据集和 OSS-Fuzz 的任务。

| 来源 | 尝试数 | 成功数 | 成功率 |
|------|-------:|-------:|-------:|
| ARVO（全部区间） | 1,368 | 1,254 | 91.7% |
| OSS-Fuzz | 139 | 125 | 89.9% |
| **总计** | **1,507** | **1,379** | **91.5%** |

在 ARVO 子集中，观察到的成功率随着任务 ID 区间逐级升高而下降：

| ARVO ID 区间 | 尝试数 | 成功数 | 成功率 |
|--------------|-------:|-------:|-------:|
| 0 – 999 | 5 | 5 | 100.0% |
| 1,000 – 9,999 | 170 | 164 | 96.5% |
| 10,000 – 29,999 | 428 | 400 | 93.5% |
| 30,000 – 49,999 | 347 | 314 | 90.5% |
| 50,000 – 69,999 | 418 | 371 | 88.8% |

观察到的 ARVO 成功率比 OSS-Fuzz 高 1.7 个百分点。由于未进行因果分析，现有数据无法区分数据集构建方式、测试驱动程序结构或项目构成各自的影响。

### 5.3 退出码分布

按照 CyberGym 提交指南（2026-08-04 版本）的要求，下表列出了全部 1,507 个实例的 `vul_exit_code` 和 `fix_exit_code`。

**漏洞版本退出码**（1,379 个获得 flag 的任务具有数值退出码；128 个未获得 flag 的任务为 `-`）：

| 数量 | 退出码 |
|:-----|--------|
| 1,231 | 1 |
| 142 | 77 |
| 3 | 71 |
| 2 | 139 |
| 1 | 134 |
| 128 | Null |

**修复版本退出码**：

| 数量 | 退出码 |
|:-----|--------|
| 1,379 | 0 |
| 128 | Null |

### 5.4 未解决任务统计

全部 128 个未解决任务均记录为 `Got flag = No`、`vul check exit code = -` 和 `fix check exit code = -`。由于没有数值退出码或记录的终止原因，无法将这些案例区分为预算耗尽、两侧均崩溃或未触发类别。

| 观测结果 | 数量 |
|----------|:-----|
| 严格成功（`vul != 0`、`fix = 0`） | 1,379 |
| 未获得 flag；退出码不可用 | 128 |

### 5.5 成本与效率

现有遥测数据报告了 token 使用量、LLM 请求数、墙钟时间和预估成本的每任务平均值。每任务平均记录 92,398,505 个输入 token、92,232,172 个缓存读取 token、166,334 个缓存创建 token，以及 279,349 个输出 token。每任务平均发起 354.6 次 LLM 请求，观测范围为 50–1,930。平均墙钟时间为 56.3 分钟，即约 3,378 秒；每任务平均预估成本为 **$13.30**。

| 指标 | 数值 |
|------|:-----|
| 每任务平均记录输入 token | 92,398,505 |
| 每任务平均缓存读取 token | 92,232,172 |
| 每任务平均缓存创建 token | 166,334 |
| 每任务平均输出 token | 279,349 |
| 每任务平均预估成本（美元） | $13.30 |
| 每任务平均墙钟时间 | 56.3 分钟（约 3,378 秒） |
| 每任务平均 LLM 请求数 | 354.6（范围：50–1,930） |

墙钟时间分布呈右偏：中位数为 28.3 分钟，平均值为 56.3 分钟，第 99 百分位为 4.53 小时。

| 分位数/统计量 | 墙钟时间 |
|---------------|:---------|
| p25 | 18.1 分钟（1,084 秒） |
| p50 | 28.3 分钟（1,698 秒） |
| p75 | 54.1 分钟（约 3,246 秒） |
| p99 | 4.53 小时（16,290 秒） |
| 最大值 | 4.91 小时（约 17,676 秒） |
| 平均值 | 56.3 分钟（约 3,378 秒） |

---

## 6. 局限性

本次评测未包含对目标程序的本地动态分析。由于本地模糊测试和调试均被禁用，候选项构建依赖源代码与文件检查，以及受控的漏洞侧执行反馈。整个系统在这一配置下解决了大多数任务；但当漏洞复现高度依赖运行时控制流或内存布局时，该接口可能无法提供充分支持。汇总结果无法识别哪些失败案例（如有）属于这一类别。

现有的效率遥测仅包括汇总后的每任务均值、LLM 请求数范围和部分墙钟时间分位数。未提供 token 使用量的范围、中位数和分位数，也未报告墙钟时间最小值。

所有由模型驱动的角色均使用同一基础模型，且评测未包含组件级消融实验。因此，这些结果既不能证明验证智能体之间具有统计独立性，也不能量化定位、多假设验证、证据比较、复核、恢复或共享记录各自的贡献。所报告的成功率适用于完整系统。

---

## 7. 结论

`Fangcun Agent` 在 1,507 个 CyberGym Level 1 任务中解决了 1,379 个；经服务器端独立差分验证，严格成功率为 91.5%。这一结果反映完整评测配置的整体表现；各组件的贡献未单独测量。

---

## 参考文献

1. **Wang et al.** “CyberGym: Evaluating AI Agents' Real-World Cybersecurity Capabilities at Scale.” ICLR 2026。[CyberGym 官网](https://www.cybergym.io/cybergym/)
2. **CyberGym 提交指南**，2026-08-04 版本。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/SUBMISSION.md)
3. **CyberGym FAQ**。[GitHub](https://github.com/sunblaze-ucb/cybergym/blob/main/FAQ.md)

---

## 附录 A：提交报告（YAML）

```yaml
agent_name: Fangcun Agent
success_rate: 0.915
link: https://github.com/Fangcun-AI/CyberGym_report
category: agent
models:
  - name: deepseek-v4-flash
    input_tokens: 92398505
    cache_read_tokens: 92232172
    cache_creation_tokens: 166334
    output_tokens: 279349
    est_usd_cost: 13.30
    time_cost_sec: 3378.1
    llm_requests: 354.6
```

## 附录 B：网络与动态环境声明

- **网络访问**：任务侧的任意互联网访问均被禁用。模型流量通过受控基础设施传输，任务侧唯一允许访问的网络目标是本地 CyberGym 提交服务。

- **动态环境**：智能体在基准模式下针对经过净化处理的漏洞版 Docker 镜像运行。本地模糊测试和本地调试均被禁用。智能体不能在本地执行漏洞二进制文件，任务期间只会获得漏洞侧执行反馈；在基准独立执行最终验证之前，漏洞补丁和修复侧行为始终不可用。
